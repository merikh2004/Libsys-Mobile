import React from 'react';
import { View, Text } from 'react-native';

const CartScreen = () => {
  return (
    <View className="flex-1 justify-center items-center bg-white">
      <Text className="text-2xl font-bold">Cart Screen</Text>
    </View>
  );
};

export default CartScreen;
